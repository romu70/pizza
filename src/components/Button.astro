---
const { id, type, value, label } = Astro.props;
---

<button id={id} type={type} value={value}>{label}</button>

<style>
  button {
    margin-top: 1rem;
    background-color: #f5e90c;
    color: black;
    border-radius: 0.5rem;
    width: 100%;
    border: none;
    font-weight: 600;
    font-size: 1.2rem;
    text-align: center;
    padding: 1.2rem;
  }

  button:hover {
    filter: brightness(0.8); /* Increase luminance */
  }

</style>
